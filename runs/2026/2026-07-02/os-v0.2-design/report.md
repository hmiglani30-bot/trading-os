# Trading OS v0.2 Design Report

**Date:** 2026-07-02  
**Workflow:** os-v0.2-design  
**Scope:** Define how prompts, source selection, outputs, and browser-readable reports work together.

## 1. Executive Summary

The first scaffold was useful as a file structure, but it was not strict enough about prompt quality, source selection, and output storage. Version 0.2 upgrades the system around three principles:

1. Prompts must be source-aware.
2. Workflows must produce one readable report, not many scattered outputs.
3. Every completed run must be saved as Markdown, rendered to HTML, and archived as PDF.

## 2. What Changed in v0.2

| Area | v0.1 issue | v0.2 fix |
|---|---|---|
| Prompt quality | Prompts were organized but not strict enough | Added source-aware prompt contracts and quality checks |
| Source usage | Data-source policy was not central enough | Added `source_registry/source_matrix.md` |
| Output storage | Output pattern was described but not enforced | Added `runs/` convention and renderer |
| Browser access | Needed a way to avoid local download clutter | Added GitHub Pages publishing workflow |
| Review loop | Improvement loop was conceptual | Added retrospective and prompt-review module |

## 3. Operating Model

```text
user input + source matrix
  -> prompt module
  -> workflow report
  -> runs/YYYY/YYYY-MM-DD/<workflow>/report.md
  -> generated report.html and report.pdf
  -> site/index.html browser index
```

## 4. Report Output Contract

Every workflow should create this folder shape:

```text
runs/YYYY/YYYY-MM-DD/<workflow-name>/report.md
runs/YYYY/YYYY-MM-DD/<workflow-name>/report.html
runs/YYYY/YYYY-MM-DD/<workflow-name>/report.pdf
```

`report.md` is the source of truth. `report.html` is the browser-readable version. `report.pdf` is the archive/shareable version.

## 5. Prompt Modules Added

| Module | Purpose |
|---|---|
| P00 Master Report Contract | Universal report structure and quality bar |
| P01 Daily Context Brief | Daily event/context summary |
| P02 Weekly Context Review | Weekly calendar and broad-context review |
| P03 Portfolio Snapshot Review | User-provided snapshot quality and exposure review |
| P04 Security Research Note | Company/ticker research note |
| P05 Chart Context Review | Chart-context documentation |
| P06 News and Catalyst Review | News/materiality review |
| P07 Event Prep | Scheduled-event preparation |
| P08 Options Overlay Scenario Review | Educational options-scenario review |
| P09 Pre-Action Memo | Written reasoning before meaningful manual action |
| P10 Red-Team Review | Challenge assumptions and missing evidence |
| P11 Retrospective and Prompt Review | Improve prompts after completed cycles |

## 6. Source Policy

The source policy is deliberately tiered:

1. Primary or official sources for precise facts.
2. Dashboards for scanning and context.
3. News and commentary for narrative, not as source-of-truth data.

Each report must include `Sources Used` and `Missing Data` sections.

## 7. Browser Output Path

After merge, enable GitHub Pages with source set to GitHub Actions. Then run the workflow named `Render and publish reports`. It will generate the HTML/PDF outputs and publish the browser index.

## 8. Current Limitations

- The GitHub connector can create text files, but binary PDF files should be generated by GitHub Actions or locally.
- Some finance-specific wording can trigger connector safety checks, so repo files use research/reporting language and avoid execution instructions.
- This system stores research outputs; it does not automate financial account actions.

## 9. Next Improvements

1. Add a real daily report using current market sources.
2. Add a real weekly review using a sanitized holdings snapshot.
3. Add stronger examples for each prompt module.
4. Add a prompt changelog and failure log.
5. Enable GitHub Pages after merge.

## 10. Sources Used

- User requirements from this chat.
- Existing Trading OS scaffold design.
- Source-policy approach discussed in prior analysis.

## 11. Missing Data

- No live portfolio snapshot included in this sample.
- No live source browsing included in this sample design run.
